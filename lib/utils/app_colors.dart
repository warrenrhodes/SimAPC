import 'package:flutter/material.dart';

/// List of the Customers app colors.
class AppColors {
  /// Transparent Color.
  static const Color transparent = Colors.transparent;

  /// Green Color.
  static const Color green = Color(0xff81d23b);

  /// Teal Color.
  static const Color teal = Color(0xff00d4a2);

  /// Turquois Color.
  static const Color turquois = Color(0xff7bebff);

  /// Blue Color.
  static const Color blue = Color(0xff19a9e5);

  /// Dark Blue Color.
  static const Color darkBlue = Color(0xff0e5c7c);

  /// Red Color.
  static const Color red = Color(0xFFfd4e11);

  /// Gray Color.
  static const Color gray = Color(0xffb2b2b2);

  /// Darker Gray Color.
  static const Color darkerGray = Color(0xff505050);

  /// Dark Gray Color.
  static const Color darkGray = Color(0xff202020);

  /// White Color.
  static const Color white = Color(0xffffffff);

  /// Red Color.
  static const Color kRedColor = Colors.red;

  /// Red Accent color.

  /// White Color.
  static const Color kWhiteColor = Colors.white;

  /// Deep Orange Color.
  static const Color kDeepOrangeColor = Colors.deepOrange;

  /// Orange with shape 600.
  static const Color kOrange600 = Color(0xFFFB8C00);

  /// Deep orange accent colors.
  static const Color kDeepOrangeAccentColor = Colors.deepOrangeAccent;

  /// Transparent Background color.
  static const Color kTransparentColor = Colors.transparent;

  /// Grey Color.
  static const Color kGreyColor = Colors.grey;

  /// Grey with shape 900.
  static const Color kGrey900 = Color(0xFF212121);

  /// Grey with shape 200.
  static const Color kGrey200 = Color(0xFFEEEEEE);

  /// Grey with shape 300.
  static const Color kGrey300 = Color(0xFFE0E0E0);

  /// Grey with shape 800.
  static const Color kGrey800 = Color(0xFF424242);

  /// Blue  Color.
  static const MaterialColor kBlueColor = Colors.blue;

  /// Light blue Accent color.
  static const Color kLightBlueAccentColor = Colors.lightBlueAccent;

  /// Blue with shape 300.
  static const Color kBlue300 = Color(0xFF64B5F6);

  /// Blue with shape 400.
  static const Color kBlue400 = Color(0xFF42A5F5);

  /// Light blue color.
  static const Color kLightBlueColor = Colors.lightBlue;

  /// Light Green color.
  static const Color kLightGreenColor = Colors.lightBlue;

  /// Green color.
  static const Color kGreenColor = Colors.green;

  /// Teal color.
  static const Color kTealColor = Colors.teal;

  /// Teal with shape 300.
  static const Color kTeal300 = Color(0xFF4DB6AC);

  /// white with light 70.
  static const Color kWhite70 = Color(0xB3FFFFFF);

  /// Light black  color.
  static const Color kLightBlackColor = Color(0x73000000);

  /// Black color.
  static const Color kBlackColor = Colors.black;

  /// Black with light 38.
  static const Color kBlack38 = Color(0x61000000);

  /// Black with light 12.
  static const Color kBlack12 = Color(0x1F000000);

  /// Black with light 26.
  static const Color kBlack26 = Color(0x42000000);

  /// Black with light 54.
  static const Color kBlack54 = Color(0x8A000000);

  /// Hexdecimal blue color.
  static const Color kBlueHexdec = Color(0xFF7BEBFF);

  /// Hexdecimal blue light  color.
  static const Color kBlueLightHexdec = Color(0xFF04CCAD);

  /// Hexdecimal blue light  color.
  static const Color kGreenLightHexdec = Color(0xFF00D4A2);

  /// RGBO blue color.
  static const Color kBlueRGBO = Color.fromRGBO(25, 169, 229, 1);

  /// RGBO green color.
  static const Color kGreenRGBO = Color.fromRGBO(0, 212, 162, 1);

  /// RGBO purple color.
  static const Color kDarkHomeRGBO = Color.fromRGBO(35, 48, 64, 1);

  //58, 189, 167, 1
  /// RGBO white light  color.
  static const Color kWhiteRGBO = Color.fromRGBO(250, 250, 250, 1);

  //255, 40, 41, 31
  /// RGBO white light  color.
  static const Color kDarkBlueDarkRGBO = Color.fromARGB(42, 63, 84, 131);
}
